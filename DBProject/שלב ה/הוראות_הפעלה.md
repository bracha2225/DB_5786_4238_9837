# שלב ה' – ממשק גרפי לניהול בסיס הנתונים הרפואי

## 📖 תיאור כללי

בשלב זה פיתחנו ממשק גרפי מלא (Web Application) המחבר בין בסיס הנתונים PostgreSQL שבנינו בשלבים הקודמים לבין משתמש קצה. האפליקציה מאפשרת ביצוע כל פעולות ה-CRUD (יצירה, קריאה, עדכון, מחיקה) על כל הטבלאות, הרצת שאילתות SQL, והפעלת פונקציות ופרוצדורות שנכתבו בשלב ד'.

---

## 🛠️ כלים וטכנולוגיות ששימשו לבניית האפליקציה

### Backend (שרת)
| כלי | גרסה | מטרה |
|-----|-------|-------|
| **Python 3** | 3.8+ | שפת תכנות לשרת |
| **Flask** | 3.x | מסגרת עבודה (Framework) לבניית REST API |
| **psycopg2-binary** | 2.x | ספריית Python לחיבור ל-PostgreSQL |

### Frontend (ממשק)
| כלי | מטרה |
|-----|-------|
| **HTML5** | מבנה הדף |
| **Vanilla CSS3** | עיצוב – כולל Glassmorphism, גראדיאנטים, אנימציות |
| **Vanilla JavaScript (ES5+)** | לוגיקת הצד הלקוח – Fetch API, DOM Manipulation |
| **Font Awesome 6** | אייקונים |
| **Google Fonts – Assistant** | גופן עברי מודרני |

### מסד הנתונים והתשתית
| כלי | מטרה |
|-----|-------|
| **PostgreSQL 17** | מסד הנתונים הרלציוני |
| **Docker + Docker Compose** | הרצה של מסד הנתונים ו-pgAdmin בסביבה מבודדת |
| **pgAdmin 4** | ממשק ניהול גרפי למסד הנתונים (http://localhost:8080) |

### דרך העבודה
האפליקציה בנויה בארכיטקטורת **Client-Server**:
- ה-**Frontend** (HTML/CSS/JS) רץ בדפדפן ושולח בקשות HTTP ל-API
- ה-**Backend** (Flask) מקבל את הבקשות, מבצע שאילתות על PostgreSQL ומחזיר JSON
- כל הלוגיקה בצד השרת – הלקוח רק מציג ומאסף קלט

---

## 🚀 הוראות הפעלה

### דרישות קדם
1. **Docker Desktop** – מותקן ופעיל
2. **Python 3** (גרסה 3.8+)
3. ספריות Python – Flask ו-psycopg2-binary

### שלב 1: התקנת ספריות Python (פעם אחת בלבד)
```bash
pip3 install flask psycopg2-binary
```

### שלב 2: הפעלת מסד הנתונים
מתיקיית השורש של הפרויקט:
```bash
docker compose up -d
```
> מסד הנתונים יהיה זמין על פורט **5433**.  
> pgAdmin יהיה זמין בכתובת [http://localhost:8080](http://localhost:8080)

### שלב 3: הרצת האפליקציה
```bash
cd "DBProject/שלב ה"
python3 app.py
```

### שלב 4: פתיחת הממשק
פתחו את הדפדפן וגשו לכתובת:

> 🌐 **http://localhost:5001**

> **הערה:** האפליקציה רצה על פורט 5001 (ולא 5000) כדי למנוע התנגשות עם שירות AirPlay של macOS.

---

## 🖥️ מסכי המערכת

### 1. לוח בקרה (Dashboard)
מציג בזמן אמת:
- סה"כ מטופלים במערכת
- אשפוזים פעילים (שטרם שוחררו)
- סה"כ אנשי סגל
- אנשי סגל פעילים

### 2. ניהול מטופלים
CRUD מלא – הוספה, צפייה, עריכה ומחיקה של מטופלים. הממשק מציג את שם הרופא האחראי (Join עם טבלת staff).

### 3. ניהול צוות רפואי
CRUD מלא לצוות הרפואי, כולל סטטוס (Active / Inactive / On Leave), מחלקה ותפקיד.

### 4. ניהול אשפוזים
רישום אשפוזים חדשים, עדכון תאריך שחרור, ומחיקה. כולל תצוגת "מאושפז פעיל" לרשומות ללא תאריך שחרור.

### 5. אלרגיות / היסטוריה רפואית / ביטוחים / אנשי קשר
כל המסכים תומכים ב-CRUD מלא עם תפריטי בחירה דינמיים לבחירת מטופל.

### 6. שאילתות ותוכניות (שלב ב' + שלב ד')
- **שאילתה 1** – מטופלים עם אלרגיה מסכנת חיים וכמות אשפוזיהם
- **שאילתה 2** – אנשי קשר לחירום לאשפוזים פעילים
- **פונקציה** – `evaluate_patient_risk` – הערכת סיכון רפואי למטופל
- **פרוצדורה** – `bulk_discharge_patients` – שחרור המוני לפי מילת מפתח

---

## 📸 צילומי מסך מומלצים להוספה

כדי להשלים את התיעוד, מומלץ לצלם ולהוסיף לתיקיית `screenshots/` את המסכים הבאים:

| שם קובץ מומלץ | מה לצלם |
|----------------|---------|
| `01_dashboard.png` | לוח הבקרה עם הנתונים הסטטיסטיים |
| `02_patients_table.png` | טבלת מטופלים עם נתונים ועמודת פעולות |
| `03_add_patient_modal.png` | חלון הוספת מטופל פתוח |
| `04_staff_table.png` | טבלת צוות רפואי עם badges של סטטוס |
| `05_admissions.png` | טבלת אשפוזים עם "מאושפז פעיל" |
| `06_queries_result.png` | תוצאות הרצת שאילתה / הערכת סיכון |

**כיצד לצלם מסך ב-Mac:** `Cmd + Shift + 4` ואז סמנו את האזור הרצוי.
